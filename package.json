{
  "name": "get-customer-id",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-accessible agent that returns full customer records from a Google Sheet by customer ID, loyalty ID, name, or phone number.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
